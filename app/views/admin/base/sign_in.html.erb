<% content_for :title, "Sign in" %>

<%= render "shared/site_header" %>

<p style="text-align: center; color: var(--fg-muted);">
  <%= button_to "Sign in with GitHub", "/auth/github", class: "button", style: "margin-top: 1rem;", form: { data: { turbo: false } } %>
</p>
